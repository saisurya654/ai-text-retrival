# Agentic Document Intelligence Platform

This project is a schema-free document intelligence platform that can ingest uploaded documents, understand their structure without predefined templates, generate a dynamic schema from content, retrieve evidence from semantic sections, draft grounded outputs, and learn from operator feedback.

The system is designed around adaptability, grounding, explainability, and maintainability rather than fixed extraction rules.

## Features

- Upload `PDF`, `PNG`, `JPG`, and `JPEG` documents
- Extract text using `pdfplumber` for text PDFs and `EasyOCR` for scanned documents
- Clean images with `OpenCV`
- Discover document type automatically
- Detect sections, headings, subheadings, key-value pairs, tables, entities, relationships, and metadata dynamically
- Build a universal document understanding JSON
- Index semantic sections using embeddings and vector search
- Retrieve grounded evidence with source references
- Generate adaptive drafts with citations
- Learn from human feedback for future drafting behavior

## End-to-End Workflow

```text
Document Upload
-> OCR / Text Extraction
-> Layout Analysis
-> Semantic Understanding
-> Dynamic Schema Generation
-> Structured JSON Creation
-> Vector Indexing
-> Grounded Retrieval
-> Draft Generation
-> Feedback Learning
```

## Universal Output

```json
{
  "document_type": "",
  "document_summary": "",
  "sections": [],
  "entities": [],
  "relationships": [],
  "metadata": {}
}
```

## Tech Stack

- Backend: `FastAPI`
- Frontend: `Streamlit`
- OCR: `EasyOCR`
- PDF text extraction: `pdfplumber`
- Image preprocessing: `OpenCV`
- NLP and semantic enrichment: `spaCy`
- Embeddings: `SentenceTransformers`
- Vector store: `FAISS`
- Testing: `pytest`

## Project Structure

```text
LegalMind-AI/
├── app/
│   ├── api/
│   ├── models/
│   ├── services/
│   └── utils/
├── ingestion/
├── extraction/
├── retrieval/
├── generation/
├── feedback/
├── evaluation/
├── frontend/
├── samples/
├── tests/
├── README.md
├── architecture.md
├── requirements.txt
├── Dockerfile
└── docker-compose.yml
```

## Run From Scratch

These steps assume you are starting on a fresh machine with VS Code and Python installed.

### 1. Clone the repository

```powershell
git clone https://github.com/saisurya654/ai-text-retrival.git
cd ai-text-retrival
```

If you are already inside the project folder, skip this step.

### 2. Open in VS Code

```powershell
code .
```

In VS Code:

- Install the Python extension
- Open the integrated terminal
- Make sure the working folder is the project root

### 3. Create a virtual environment

```powershell
python -m venv .venv
```

### 4. Activate the virtual environment

```powershell
.venv\Scripts\Activate.ps1
```

If PowerShell blocks script execution, run:

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
```

Then activate again:

```powershell
.venv\Scripts\Activate.ps1
```

### 5. Install dependencies

```powershell
pip install --upgrade pip
pip install -r requirements.txt
```

### 6. Start the FastAPI backend

Run this in the first terminal:

```powershell
uvicorn app.main:app --reload
```

Backend URLs:

- API root: [http://localhost:8000](http://localhost:8000)
- Swagger docs: [http://localhost:8000/docs](http://localhost:8000/docs)

### 7. Start the Streamlit frontend

Open a second terminal in VS Code, activate the same environment, and run:

```powershell
.venv\Scripts\Activate.ps1
streamlit run frontend/streamlit_app.py
```

Frontend URL:

- UI: [http://localhost:8501](http://localhost:8501)

### 8. Use the application

1. Open the Streamlit UI.
2. Upload a supported document.
3. Copy the generated `document_id`.
4. Open `Document Understanding` and run understanding.
5. Use `Evidence Explorer` for semantic retrieval.
6. Use `Draft Generator` to create grounded outputs.
7. Use `Feedback Review` to store operator edits.
8. Use `Analytics Dashboard` to inspect evaluation metrics.

## API Endpoints

- `POST /upload`
- `POST /extract`
- `POST /retrieve`
- `POST /generate`
- `POST /feedback`
- `GET /documents`
- `GET /documents/{document_id}`
- `GET /history`
- `GET /evaluation`
- `GET /health`

## Example Development Commands

Run tests:

```powershell
pytest -q
```

Re-run backend after code changes:

```powershell
uvicorn app.main:app --reload
```

Re-run frontend after code changes:

```powershell
streamlit run frontend/streamlit_app.py
```

## Docker

Build and run with Docker Compose:

```powershell
docker-compose up --build
```

This starts:

- FastAPI on port `8000`
- Streamlit on port `8501`

## Sample Inputs and Outputs

Here is an example of the input document format and the structured JSON output generated by the platform.

### Sample Input Document
From [sample_project_report.txt](file:///c:/ai%20intern%20task%20ambitio/samples/input_docs/sample_project_report.txt):
```text
PROJECT STATUS REPORT

Prepared By: Meera Shah
Date: 12/05/2025
Project: Cloud Migration Program

1. Executive Summary
The migration program is 80 percent complete. Infrastructure cutover has finished for the first two regions.

2. Risks
Vendor dependency remains unresolved. Final security approval is pending.

3. Action Items
Owner | Task | Due Date
Ravi | Obtain vendor sign-off | 20/05/2025
Nina | Complete security review | 22/05/2025
```

### Sample Document Understanding Output
The schema-free understanding output is dynamically structured as a JSON payload:
```json
{
  "document_id": "doc_sample001",
  "document_type": "Project Status Report",
  "document_summary": "This document appears to be 'Project Status Report' and is organized around sections such as Project Status Report, 1. Executive Summary, 2. Risks, 3. Action Items.",
  "confidence": 0.93,
  "dynamic_schema": {
    "schema_name": "dynamic_document_schema",
    "inferred_fields": [
      "project_status_report",
      "prepared_by",
      "date",
      "project",
      "executive_summary",
      "risks",
      "action_items"
    ],
    "field_descriptions": {
      "prepared_by": "Dynamically discovered field derived from document content: Prepared By",
      "project": "Dynamically discovered field derived from document content: Project"
    },
    "confidence": 0.78
  },
  "sections": [],
  "entities": [],
  "relationships": [],
  "metadata": {
    "page_count": 1,
    "section_count": 4,
    "entity_count": 6
  }
}
```

### Sample Grounded Draft Output
The draft generator uses section-level citations and claims matching:
```json
{
  "draft_type": "Adaptive Grounded Draft",
  "inferred_document_type": "Project Status Report",
  "summary": {
    "title": "Summary",
    "content": "For the request 'Summarize project risks and actions.', the uploaded material is best understood as 'Project Status Report'. Grounded evidence suggests: This document summarizes project progress, risks, and next actions.",
    "evidence_chunk_ids": [
      "doc_sample001_p1_c1_s1"
    ],
    "traces": [
      {
        "claim": "Vendor dependency remains unresolved. Final security approval is pending.",
        "source_chunk": "doc_sample001_p1_c1_s1",
        "page": 1
      }
    ]
  }
}
```

## Assumptions and Tradeoffs

### 1. Document Ingestion & OCR
- **Assumption**: Digital PDFs are clean enough for direct text extraction using `pdfplumber`. Noisy, image-only, or scanned PDFs are converted to image pages first, which are then parsed via `EasyOCR`.
- **Tradeoff**: Running OCR on every page can be CPU/memory intensive. The system uses a fast-path strategy: if direct text extraction succeeds, it skips OCR; otherwise, it triggers OpenCV preprocessing (denoising, grayscaling, thresholding) followed by EasyOCR fallback.

### 2. Extraction & NLP Heuristics
- **Assumption**: The system can run without active internet or expensive API keys. Thus, it utilizes a local `spaCy` parser with heuristic regex rules as a robust fallback.
- **Tradeoff**: Heuristics are faster and private, but less flexible than LLMs. We offset this limitation by building a feedback-driven extraction post-processor that corrects patterns directly based on human-in-the-loop edits.

### 3. Vector Retrieval & Grounding Control
- **Assumption**: Dense embeddings using `SentenceTransformers` and `FAISS` are sufficient to capture semantic sections.
- **Tradeoff**: To prevent hallucinations, if the semantic search similarity is below a certain threshold or document understanding confidence is low, the draft generator explicitly logs a "Grounding Risk" in the draft sections instead of making unsupported assumptions.

### 4. Feedback & Dynamic Correction Loop
- **Assumption**: Operator modifications can be generalized into terminology replacement rules (e.g., standardizing a name, project classification, or document type).
- **Tradeoff**: Simple string mappings are fast and deterministic but might over-correct. We mitigated this by applying object-ID tracking and precise word/context checks during feedback execution.

---

## Evaluation Approach and Results

We measure the platform's accuracy and robustness using an automated `EvaluationEngine` in `evaluation/metrics.py`.

### Metrics Evaluated:
1. **OCR Accuracy**: Mean confidence score returned by the OCR layer.
2. **Extraction Accuracy**: Ratio of successfully populated semantic fields vs total potential fields.
3. **Precision & Recall @ K**: Verifies search relevance of the vector database passages.
4. **Groundedness & Citation Coverage**: Measures the proportion of draft recommendations backed by page/chunk references.
5. **Improvement Rate**: Checks how well the system adapts as feedback events increase.

To inspect the latest evaluation metrics, run the backend and frontend, navigate to **Analytics Dashboard**, and click **Refresh Evaluation**.

---

## Development Notes

- `.venv` is intentionally ignored and should never be committed.
- Runtime-generated files under `data/` are ignored.
- The architecture is ready for stronger layout parsers and multimodal models later without changing the external workflow.

